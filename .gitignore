node_modules
dist
.wrangler
.env
.dev.vars
*.local
.DS_Store

# ccrun state (per-machine, not versioned)
.ccrun/
/.multiclaude/

# Playwright run artifacts (screenshots we intentionally keep live under docs/runs/)
/test-results/
/playwright-report/
